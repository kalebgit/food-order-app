

function CirclesContainer({children}){
    return (
        <section className="grid md:grid-cols-5 md:grid-rows-2">
            {children}
        </section>
    )
}

export default CirclesContainer;